services:
  - type: web
    name: archery-team-site
    runtime: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: SESSION_SECRET
        generateValue: true
